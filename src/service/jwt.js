// import jwt from 'jsonwebtoken'

const verifyToken = (json) => {
/*     try {
        const decodedToken = jwt.verify(json,'jsonwebtoken')
        if (decodedToken.auth == 'admin')
            return true
        return false
    } catch (err) {

    }
 */
}

export default verifyToken